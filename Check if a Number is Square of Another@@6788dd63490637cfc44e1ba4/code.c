#include <stdio.h>

int main() {
    int num1, num2;
    if (num2 == num1 * num1) {
        printf("Yes");  // num2 is the square of num1
    } 

    else{
        printf("No");  // num2 is not the square of num1
    }

    return 0;
}
